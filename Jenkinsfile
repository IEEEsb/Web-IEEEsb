node{
    ws("/home/web/test"){
            stage 'Jenkins TEST'
                    sh'echo "Hello, this is my first pipeline"'
                    sh 'echo $PATH'
                    sh 'node --version'
                    sh 'nvm --version'
                    sh 'gulp --version'            
    }
}
